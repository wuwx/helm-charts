apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nacos-server.fullname" . }}-test-connection"
  labels:
    {{- include "nacos-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nacos-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
