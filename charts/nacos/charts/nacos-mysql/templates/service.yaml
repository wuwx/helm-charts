apiVersion: v1
kind: Service
metadata:
  name: {{ include "nacos-mysql.fullname" . }}
  labels:
    {{- include "nacos-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3306
      protocol: TCP
  selector:
    {{- include "nacos-mysql.selectorLabels" . | nindent 4 }}
