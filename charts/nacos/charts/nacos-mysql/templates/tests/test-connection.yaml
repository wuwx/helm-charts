apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nacos-mysql.fullname" . }}-test-connection"
  labels:
    {{- include "nacos-mysql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nacos-mysql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
